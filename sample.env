# === Base ===
# Choose any absolute path you own (no trailing slash).
BASE_DIR=/srv/weather-stream
LOCAL_TZ=America/Chicago

# === YouTube API ===
# These will live under BASE_DIR unless you provide absolute paths.
GOOGLE_CLIENT_SECRETS=${BASE_DIR}/client_secret.json
GOOGLE_TOKEN_FILE=${BASE_DIR}/token.json

# Optional reusable stream id (recommended to pre-create one in YouTube Studio)
YT_STREAM_ID=

# The RTMP ingest base (YouTube)
RTMP_INGEST=rtmp://a.rtmp.youtube.com/live2

# === Stream Key (kept OUTSIDE env) ===
# Path to a private file (0600) that contains ONLY the stream key string.
YT_STREAM_KEY_FILE=/home/<youruser>/.config/weather-stream/yt_key

# === Camera / FFmpeg ===
# Reolink RLC-810WA main or sub stream (recommend main + downscale for YT)
RTSP_URL=rtsp://USERNAME:PASSWORD@192.168.1.50:554/h264Preview_01_main

VIDEO_WIDTH=1280
VIDEO_HEIGHT=720
VIDEO_FPS=30
VIDEO_BITRATE=2500k
KEYINT=60
AUDIO_SAMPLE_RATE=44100
AUDIO_BITRATE=128k

# === Orchestration ===
ROTATION_HOURS=12
HEALTH_CHECK_INTERVAL_SECS=10
FFMPEG_EXIT_RETRY_DELAY_SECS=5
MAX_CONSECUTIVE_FFMPEG_ERRORS=20

# === Logs ===
LOG_DIR=${BASE_DIR}/logs
